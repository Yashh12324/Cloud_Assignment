name: churn-prod-env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.9
  - scikit-learn=1.3.0
  - pandas=1.5.3
  - pip
  - pip:
    - mlflow==2.9.2
    - azureml-mlflow # <-- Add this line back
    - fsspec
    - adlfs
    - azureml-fsspec